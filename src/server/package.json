{
  "name": "process-identity-token-server",
  "version": "1.0.0",
  "description": "Processes an identity token from the chrome extension in ../src",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
